# Expo/React Native resolve native modules by walking node_modules, which
# breaks under pnpm's default symlinked layout. Hoisting keeps Metro happy.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
