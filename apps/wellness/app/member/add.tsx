import { View } from "react-native";

/**
 * Placeholder route for the centre tab. The button never navigates here --- it
 * opens the quick-log sheet --- but expo-router needs a file for the slot.
 */
export default function Add() {
  return <View />;
}
